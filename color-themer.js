import { colord } from "colord";

// applyTheme: function that applies a theme to the document
// takes the following arguments:
//  - theme: the theme to apply
//  - duration: the duration of the transition
//  - onFinished: a callback to call when the transition is finished: passes the new theme object as an argument
//  - onProgress: a callback to call during each frame of the transition: passes the theme object and the progress (0-1) as arguments

// module.exports = generate;
